

# 📘 Документация к анализатору (Вариант 113311)

## Описание программы
Данная программа реализует лексический и синтаксический анализ модельного языка программирования. 
Она работает в два прохода:
1.  **Scanner**: Преобразует исходный текст в последовательность токенов (пар `тип, индекс`).
2.  **Parser**: Проверяет последовательность токенов на соответствие грамматике методом рекурсивного спуска.

## Структура лексем
* **Тип 1 (TW)**: Ключевые слова (`if`, `while`, `read`, `as`...).
* **Тип 2 (TL)**: Разделители и знаки операций (`{`, `}`, `+`, `<>`).
* **Тип 3 (mTN)**: Числовые константы (включая `10H`, `3.14`).
* **Тип 4 (mTI)**: Идентификаторы пользователей.

## Объяснение основных методов
* `scan()`: Основной цикл лексического анализатора. Реализует конечный автомат.
    * Состояние `H`: Начальное.
    * Состояние `I`: Чтение идентификаторов.
    * Состояние `N`: Чтение чисел (поддерживает суффиксы H, O, B).
    * Состояние `C`: Пропуск комментариев `/* ... */`.
* `parse()`: Запускает синтаксический разбор, вызывая функцию `block()`.
* `block()`: Обрабатывает тело программы `{ ... }`.
* `statement()`: Распознает операторы языка (`read`, `write`, `if`, `while`, присваивание).
* `expr()`: Разбирает арифметические и логические выражения. Обрабатывает цепочки операций (например, `a + b - c`).

---

## 🧪 Тестовые наборы (Tests)

Ниже приведены сценарии для файла `input.txt`.

### Тест №1: Основной функционал (Позитивный)
Проверяет циклы, ввод-вывод и арифметику.
```text
{
  read(n);
  sum as 0;
  while n > 0 do
  {
    sum as sum + n;
    n as n - 1;
  };
  write(sum);
}

```

**Ожидаемый результат**: `[УСПЕХ] Синтаксис верный!`

### Тест №2: Сложные типы данных (Позитивный)

Проверяет распознавание чисел в разных системах счисления, согласно заданию.

```text
{
  binVal as 101B;
  hexVal as 1FAH;
  realVal as 3.1415;
  octVal as 77O;
}

```

**Ожидаемый результат**: `[УСПЕХ] Синтаксис верный!`

### Тест №3: Вложенные условия (Позитивный)

Проверяет рекурсивный вызов операторов `if`.

```text
{
  if a > b then
    if a > c then
      max as a
    else
      max as c
  else
    max as b;
}

```

**Ожидаемый результат**: `[УСПЕХ] Синтаксис верный!`

### Тест №4: Ошибка присваивания (Негативный)

В данном варианте присваивание выполняется через `as`, а не `=`.

```text
{
  a = 10;
}

```

**Ожидаемый результат**: `[ОШИБКА]: Ожидал 'as', получил (2, 17)` (Код 17 соответствует знаку `=`)

### Тест №5: Незакрытый комментарий (Негативный)

```text
{
  x as 5;
  /* Это комментарий, который никогда не кончится...
}

```

**Ожидаемый результат**: `[ОШИБКА]: Незакрытый комментарий`

### Тест №6: Ошибка структуры (Негативный)

Отсутствует ключевое слово `then` в условии.

```text
{
  if x > 5 x as 0;
}

```

**Ожидаемый результат**: `[ОШИБКА]: Ожидал 'then', получил (4, 1)`

---
